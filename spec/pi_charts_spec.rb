require "spec_helper"

RSpec.describe PiCharts do
  it "has a version number" do
    expect(PiCharts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true) # best testing ever
  end
end
